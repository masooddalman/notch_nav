name: notch_nav
description: "A highly customizable Flutter bottom navigation bar with a notch-style pop-up indicator supporting circle, square, and diamond shapes with gradient fills."
version: 0.1.0
homepage: https://github.com/masooddalman/notch_nav
repository: https://github.com/masooddalman/notch_nav
issue_tracker: https://github.com/masooddalman/notch_nav/issues

environment:
  sdk: ^3.11.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
